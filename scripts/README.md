# Spotify Playlist Utility Scripts

A lot of these utility scripts are called as part of the top level `Makefile` workflows
or other scripts so they are tucked away here to clean up the directory structure.

Some of the scripts still at top level are because I may call them explicitly so it's just quicker
on a day-to-day basis to leave them at the top level for convenience.
